<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="5dp"
    android:layout_marginTop="5dp"
    android:background="@color/primaryPale1"
    android:paddingBottom="5dp"
    android:paddingEnd="30dp"
    android:paddingStart="30dp"
    android:paddingTop="5dp">

    <ImageView
        android:id="@+id/chat_log_new_contact"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_add_black_100px"
        android:tint="@color/primaryDark4"
        app:layout_constraintEnd_toStartOf="@id/chat_log_blocked_name"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/chat_log_blocked_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:gravity="center"
        android:paddingBottom="5dp"
        android:paddingTop="5dp"
        android:text="@string/add_to_contact"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toEndOf="@id/chat_log_new_contact" />


</androidx.constraintlayout.widget.ConstraintLayout>